jest.disableAutomock();

import {
  graphql,
  GraphQLSchema,
  GraphQLObjectType,
  GraphQLList,
  GraphQLScalarType,
  GraphQLString,
} from 'graphql';

import TypeComposer from '../../../typeComposer';


describe('Resolver', () => {
  xit('should add args', () => {

  });

  xit('should remove args', () => {

  });

  xit('compose resolve method with middlewares', () => {

  });

  xit('can return array GraphQLList', () => {

  });
});
